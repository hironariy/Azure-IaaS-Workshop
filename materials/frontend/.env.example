# =============================================================================
# Frontend Environment Configuration - LOCAL DEVELOPMENT ONLY
# =============================================================================
# This file is for LOCAL DEVELOPMENT using `npm run dev`
#
# PRODUCTION: Configuration is loaded from /config.json created by Bicep
# See: /design/FrontendApplicationDesign.md - Workshop Configuration: Runtime Config Pattern
#
# Copy this file to .env.local for local development
# NEVER commit .env.local to version control
# =============================================================================

# =============================================================================
# Microsoft Entra ID - Frontend SPA App Registration
# =============================================================================
# Get from Azure Portal > App registrations > Frontend SPA
VITE_ENTRA_CLIENT_ID=your-frontend-spa-client-id-here
VITE_ENTRA_TENANT_ID=your-tenant-id-here
VITE_ENTRA_REDIRECT_URI=http://localhost:5173

# =============================================================================
# Backend API App Registration (for token audience)
# =============================================================================
# Get from Azure Portal > App registrations > Backend API
VITE_API_CLIENT_ID=your-backend-api-client-id-here

# =============================================================================
# API Configuration
# =============================================================================
# Not needed in dev - Vite proxy handles it (see vite.config.ts)
# VITE_API_BASE_URL=http://localhost:3000

# =============================================================================
# PRODUCTION CONFIGURATION REFERENCE
# =============================================================================
# In production (Azure Web tier VMs), configuration is loaded from /config.json
# which is created by Bicep CustomScript:
#
# {
#   "ENTRA_TENANT_ID": "<from main.bicepparam>",
#   "ENTRA_FRONTEND_CLIENT_ID": "<from main.bicepparam>",
#   "ENTRA_BACKEND_CLIENT_ID": "<from main.bicepparam>",
#   "API_BASE_URL": "/api"
# }
#
# See: /materials/bicep/modules/compute/web-tier.bicep
# =============================================================================
